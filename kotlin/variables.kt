fun main(){
    println("Kotlin Variables")
    /**
     * Staticall-typed language. type resolved at compile time and never changes
     * Optional to declare type since compiler infers type
     */
    var width: Int = 12
    var length: Double = 2.5
    //mutable and immutable variables
    var score = 10 //mutable
    val name = "Sam" //immutable

    /** NUMERALS
     * Kotlin keeps numbers as primitives but lets you call methods on numbers as if they were objects
     */
    println(2.times(5))
    /** STRINGS
     * 
     */
    /**     Type checks and Casts
     * 
     */
}