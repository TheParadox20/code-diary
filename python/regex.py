import re
"""
A regular expression pattern can be defined using any ASCII character or number, case sensitive
Regexes allow more special characters(meta) to allow more complex patterns to be described

"""